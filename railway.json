{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "buildCommand": "npm install --force && npm run build"
  },
  "deploy": {
    "startCommand": "npx next start -p ${PORT:-80}",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  }
}
